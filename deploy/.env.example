# docker compose 用。cp .env.example .env 后按需修改。

PUID=1000
PGID=1000
TZ=Asia/Shanghai

# 儿童端 nginx 对外端口。NAS 的 80/443 常被占用，默认 8080。
WEB_PORT=8080

# Jellyfin 端口绑定。
#   家庭 NAS（内网可信）：0.0.0.0:8096   —— 任意设备可开管理台
#   公网服务器：           127.0.0.1:8096 —— 仅本机，管理台走 SSH 隧道
JELLYFIN_BIND=0.0.0.0:8096
